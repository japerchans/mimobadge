# UX削減会議

## 目的

介護職員が「確認待ちを開く → 内容を確認する → 記録を確定する」を迷わず終えられることを最優先にする。表示要素は、現在の判断に必要か、記録の安全性と根拠確認に必要なものだけにする。

## 2026-09-15 初回レビュー

不要機能の削減、タスクフロー、操作の直感性を担当する3つの独立エージェントで現行実装を確認した。3者で一致した項目を優先し、追加機能の案は採用しなかった。

| 優先度 | ページ             | 判断                                                                                       | 理由                                                                    | 根拠                                                        |
| ------ | ------------------ | ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------- | ----------------------------------------------------------- |
| P0     | `/`・`/processing` | 確認待ちを入口に統合し、重複する録音一覧を二次導線へ移す                                   | 同じ未確認録音と「デモ録音を追加」が2画面にあり、開始地点が競合している | `components/dashboard.tsx`、`components/facility-views.tsx` |
| P0     | `/processing/:id`  | 下書きと反映項目を主表示にし、5段階表示・全文・記録しない会話・過去情報を初期表示から外す  | 1件の確定に必要な判断が複数の領域へ重複している                         | `components/recording-workspace.tsx`                        |
| P0     | `/processing/:id`  | 主操作を「記録を確定」1つにする。下書き保存は自動化し、作り直し操作を外す                  | 保存と確定が競合し、再生成が職員の編集を上書きしうる                    | `components/recording-workspace.tsx`                        |
| P0     | モバイル共通       | メニュー状態とアイコンを一致させ、開いたメニューを画面上へ重ねる                           | 現在は閉じた状態で閉じるアイコンが出て、開くと本文を横へ押す            | `components/workspace.tsx`、`app/globals.css`               |
| P1     | `/residents/:id`   | 「記録」「プロフィール」の2領域へ統合し、右側の重複プロフィールを削除する                  | 同じプロフィール情報が履歴、右欄、タブに重複している                    | `components/resident-workspace.tsx`                         |
| P1     | 共通ナビ           | 主ナビを「確認待ち」「入居者」「申し送り」に絞り、職員・設定・監査をユーザーメニューへ移す | 日常業務と管理情報が同じ階層で競合している                              | `components/workspace.tsx`                                  |
| P1     | 状態表示           | 「確認が必要」「処理中」「確定済み」の3語へ統一する                                        | 「未確認」「未処理」「要確認」が同じ判断を別の言葉で表している          | `components/shared.tsx` と各一覧                            |
| P2     | `/handoffs`        | 今日の記録がある入居者だけを表示し、履歴と保存済み資料を別導線へ移す                       | 現在の申し送り作成に不要なカードと履歴が同時に見える                    | `components/facility-views.tsx`                             |

## ページごとの主操作

| ページ   | 主操作                                        |
| -------- | --------------------------------------------- |
| 確認待ち | `確認する`                                    |
| 録音確認 | `記録を確定`                                  |
| 確定後   | `次の確認へ`                                  |
| 入居者   | 未確認があれば `確認する`、なければ記録を読む |
| 申し送り | `申し送りを作成`                              |

## 段階表示へ移す情報

- 元の会話全文
- 項目ごとの根拠発言
- 記録しない会話
- 過去の記録と変更履歴
- 技術情報、監査情報、デモ用操作

## 維持する情報

- 入居者の明示的な紐付け
- AI候補の個別編集・除外
- 根拠発言への到達手段
- 確定前の確認と確認者の記録
- 介護記録、プロフィール、申し送りの成果物

## 検証方法

- 確認待ちから確定までの画面遷移数と主要ボタン数を数える
- 各ページに強い主操作が1つだけ表示されることを確認する
- デスクトップと390px幅で同じ次の行動が理解できることを確認する
- 根拠、履歴、監査情報へ必要時に到達できることを確認する
- 確定後の修正率と、確認1件あたりの所要時間を追う
